export interface Index {
  /*
   * Error message when loading img failed
   *
   * @default string
   */
  error?: string
  /*
   * Url to the lazy loading img
   *
   * @default
   */
  loading?: string
}
